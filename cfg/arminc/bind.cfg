// Remove all existing binds.
unbindall

// Updates game keyboard layout to the current Windows keyboard setting.
key_updatelayout

// Slot binds.
bind "1"             "slot1"                                   // Equip: Primary weapon.
bind "2"             "slot2"                                   // Equip: Secondary weapon (pistols).
bind "3"             "slot3"                                   // Equip: Knife, Zeus or Hands (dangerzone).
bind "4"             "slot5"                                   // C4
bind "5"             "slot7"                                   // Flash
bind "6"             "slot8"                                   // Smoke
bind "7"             "slot10"                                  // Molotov
bind "8"             ""
bind "9"             ""
bind "0"             ""

// Movement binds.
bind "a"             "+moveleft"                               // Strafe left.
bind "d"             "+moveright"                              // Strafe right.
bind "s"             "+back"                                   // Move back.
bind "w"             "+forward"                                // Move forward.

// Chat binds.
bind "u"             "messagemode2"                            // Team messages (chat only with the team).
bind "y"             "messagemode"                             // Broadcast and Team messages (chat with all).

// Radio binds.
bind "c"             "radio3"                                  // Report Radio Message.
bind "x"             "radio2"                                  // Standard Radio Message.
bind "z"             "radio1"                                  // Command Radio Message.

// Other letter keys.
bind "b"             "buymenu"
bind "e"             "+use;+sound"
bind "f"             "+lookatweapon;+insp" 
bind "g"             "drop"                                    
bind "h"             ""
bind "i"             ""
bind "j"             ""
bind "k"             ""
bind "l"             ""
bind "m"             "teammenu"
bind "n"             ""
bind "o"             ""
bind "p"             ""
bind "q"             "+voicerecord"
bind "r"             "+reload"
bind "t"             "+spray_menu"
bind "v"             ""                          

// Special keys.
bind "escape"        "cancelselect"
bind "tab"           "+netgraph"
bind "capslock"      ""
bind "shift"         "+speed"                                  // Walk (slow; no sound).
bind "rshift"        ""
bind "ctrl"          "+duck"
bind "rctrl"         ""
bind "alt"           "+slot4"                            // Cycle nades
bind "ralt"          ""
bind "space"         "+jump"
bind "enter"         ""
bind "backspace"     ""
bind "ins"           ""
bind "del"           ""
bind "home"          ""
bind "end"           ""
bind "pgup"          "exec arminc/bigcrosshair.cfg"
bind "pgdn"          "exec arminc/crosshair.cfg"
bind "scrolllock"    ""

// Punctuation keys.
bind "`"             "toggleconsole"
bind ","             ""
bind "."             ""
bind "/"             ""
bind "semicolon"     ""
bind "'"             ""
bind "\"             ""
bind "["             ""
bind "]"             ""
bind "-"             ""
bind "="             ""

// Arrow keys.
bind "uparrow"       ""
bind "downarrow"     ""
bind "leftarrow"     ""
bind "rightarrow"    ""

// Mouse keys.
bind "mwheelup"      "+jump"
bind "mwheeldown"    "+jump"
bind "mouse1"        "+attack;+tap"
bind "mouse2"        "+attack2+sound"
bind "mouse3"        "clutchmode"
bind "mouse4"        ""
bind "mouse5"        ""

// Function keys.
bind "f1"            ""
bind "f2"            ""
bind "f3"            ""
bind "f4"            ""
bind "f5"            ""
bind "f6"            ""
bind "f7"            ""
bind "f8"            ""
bind "f9"            ""
bind "f10"           ""
bind "f11"           ""
bind "f12"           "" 

// Numpad.
bind "numlock"       ""                                        
bind "kp_slash"      "say gh"                                 
bind "kp_multiply"   "say glhf"         
bind "kp_minus"      "kill"      
bind "kp_plus"       "hax"       
bind "kp_enter"      ""           
bind "kp_del"        ""             
bind "kp_ins"        ""         
bind "kp_end"        "+micspam"              
bind "kp_downarrow"  ""             
bind "kp_pgdn"       ""          
bind "kp_leftarrow"  ""             
bind "kp_5"          ""            
bind "kp_rightarrow" ""              
bind "kp_home"       ""           
bind "kp_uparrow"    ""
bind "kp_pgup"       ""     

// Pressing the +use key will open the buy menu if in a buy zone (just as if the 'buy' key is pressed).
// Default: 1
cl_use_opens_buy_menu "0"

// Name of the binding to enable mouse selection in the scoreboard.
// Default: +attack2
cl_scoreboard_mouse_enable_binding "+attack2"

// Follow grenade key.
// Default: 0
//
// >> The key used to follow grenades thrown by a player which is observed.
//
// <> Values:
// 0: LeftALT
// 1: LeftSHIFT
// 2: +reload key (default key: 'R').
cl_spec_follow_grenade_key "2"

// The Duck method.
// Default: 0
//
// <> Values:
// 0: Hold to duck.
// 1: Press to toggle duck.
option_duck_method "0"

// The Walk method.
// Default: 0
//
// <> Values:
// 0: Hold to walk.
// 1: Press to toggle walk.
option_speed_method "0"

// Joystick controller support.
// Default: 0
joystick "0"

// Prevents any and all joystick input for cases where a piece of hardware is incorrectly identified as a joystick and sends bad signals.
// Default: 0
//
// <i> The value '1' is equivalent with the Launch Option: '-nojoy'.
joystick_force_disabled "1"

// Sets controllers enabled/disabled just before the config is written.
// Default: 1
joystick_force_disabled_set_from_options "1"